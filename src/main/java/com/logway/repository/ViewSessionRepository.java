package com.logway.repository;

import com.logway.entity.ViewSession;
import org.springframework.stereotype.Repository;

@Repository
public interface ViewSessionRepository extends BaseRepository<ViewSession, Long> {
}