package com.logway.repository;

import com.logway.entity.PageSession;
import org.springframework.stereotype.Repository;

@Repository
public interface PageSessionRepository extends BaseRepository<PageSession, Long> {
}